# chess_puzzle

チェスの駒を使ったマッチ3系パズル。8x8の盤面に駒がランダムに並び、条件を満たす並びを作ると消える。
白も黒も自由に動かせる。

## 現在の状態（2026-08-09）

MVP実装済み。Web版と iOS版（実機で動作確認済み）がある。
バランス調整を経て連鎖の暴走は解消（平均8.8連鎖 → 1.5連鎖）、ノルマ制で終了条件も入った。

**遊べるモードは6×6の2つ。盤面もルールも同じで、1ゲームの尺だけが違う**
（みじかめ 約3分 / じっくり 約5分）。ノルマの逆算は済んでいる。

## 作業ルール（2026-08-05 shota 指定）

**このファイルに「検証済み」「解決済み」と書いてあっても、shota から指摘があったら
必ず最初から検証し直すこと。** ここに書いてあるのは過去の調査結果であって、証拠ではない。

実際にあった失敗:

> 課題10に「『同色に動ける』は誤解だった（検証済み）」と書いたあと、同じ指摘を再度受けた。
> ロジックは確かに正しかったが、真因は別（`♟` が絵文字として描画され白ポーンが黒く見えていた）。
> 「検証済み」と書いた自分のメモを根拠にして、描画側を調べるのが1往復遅れた。

指摘を受けたときの順番:

1. まず**再現データを取る**（ロジックの不変条件を大量サンプルで検証する）
2. ロジックが正しくても**そこで止めない**。データが正しいのに見え方がおかしいなら、
   描画・フォント・CSS を疑う
3. 「誤解でした」で終わらせず、**誤解を生んだ表示そのものを直す**

## 確定仕様

### モード（`src/rules.js` の VARIANTS）

盤面サイズ・ランクの区分・出現率・ノルマをまとめて切り替える。

**遊べるのは6×6の2つだけ**（`selectable: true`）。8×8はルールとしては残してあるが選択肢から外した。
合法手が1手あたり263手あり認知の負荷が高すぎたため（6×6は87手）。
`tools/` の分析では今でも `standard` を指定して比較できる。

遊べる2つは**盤面も出現率もルールも同じで、ノルマの上がり方だけが違う**。
プレイヤーから見れば「1ゲームを何分にするか」の選択になる。

| モード | 初期ノルマ | 上がり方 | 上手いプレイヤー | 腕の差 |
|---|---|---|---|---|
| みじかめ（`compact`） | 350 | x1.7 | 7ラウンド40ターン（2.9分） | 2.00倍 |
| じっくり（`compactLong`） | 350 | x1.42 | 11ラウンド70ターン（5.0分） | 2.33倍 |

**初期ノルマは2つで揃えてある。動かすのは上がり方だけ。**
初期ノルマを上げると初見の1ラウンド落ちが増えるだけで（350で20% / 400で30%）、
尺の調整には向かない。尺は上がり方で刻む。

**8×8 / ランク3段階**（選択不可・比較用）

| ランク | 駒 | 出現率 |
|---|---|---|
| 1 | ポーン | 30% |
| 2 | ナイト / ビショップ | 30%（各15%） |
| 3 | ルーク | 30% |
| ワイルド | クイーン / キング | 10%（各5%） |

初期ノルマ300。

**6×6 / ランク2段階**（遊ぶのはこれ）

| ランク | 駒 | 出現率 |
|---|---|---|
| 1 | ポーン | 45% |
| 2 | ルーク / ビショップ / ナイト | 45%（各15%） |
| ワイルド | クイーン / キング | 10%（各5%） |

初期ノルマ450。

どのモードもランクごとの出現率は揃えてある。偏らせるとそのランクが3つ揃う確率が上がる。

ランクは駒を作るときに `piece.rank` として埋め込む。盤面を見る側はモードを知らなくて済む。
盤面サイズは定数ではなく `board.length` から取る。

### 空振り禁止（clearingMovesOnly）

**何も消えない手は指せない**（`moves.js` の `playableSquares`）。

空振りを許すと「ターンを捨ててレア役を仕込む」のが最適解になっていた
（実測で空振り35手を挟むとスコアが2.4倍）。禁止するとその抜け道が消える。

副作用がとても大きい: **1ターンの選択肢が91手 → 14〜19手**になり、
長らく課題だった「選択肢が多すぎる」がほぼ解決した。
手詰まり（消せる手が無くなる）は0.3〜1.1%で、詰みゲーにはなっていない。

チュートリアルだけはこの制限を外している（空振りの手を見せるステップがあるため）。

### ポーンの昇格（プロモーション）

盤面に `promoteAfter` ターン生き残ったポーンは、ワイルド（クイーンかキング）に昇格する
（`game.js` の `agePieces`）。チェスのプロモーションに相当する。

**狙い**: ワイルドを「降ってくるもの」ではなく「消さずに守って作るもの」にする。
これで初めて「取る／揃える」と「育てる」がぶつかる。

駒には `age`（盤面に居続けたターン数）を持たせている。1手の処理が落ち着いたところで
1回だけ歳を取らせ、昇格させる。昇格でまた揃うことがあるので、その場合は連鎖として続く。

`npm run promotion-sweep` の実測（6×6・空振り禁止あり・各1500ゲーム）:

| 昇格 | 選択肢 | 昇格/ターン | ワイルド率 | 混合ロイヤル | クイーン | キング | スコア中央 |
|---|---|---|---|---|---|---|---|
| なし | 14 | 0.00 | 1.7% | 167ゲームに1回 | 750 | 375 | 11,257 |
| 8ターン | 19 | 0.80 | 8.5% | 2ゲームに1回 | 31 | 12 | 21,911 |
| **10ターン（現行）** | **18** | **0.54** | **6.7%** | **5ゲームに1回** | **50** | **19** | **16,204** |
| 12ターン | 17 | 0.39 | 5.5% | 9ゲームに1回 | 83 | 39 | 14,350 |
| 14ターン | 16 | 0.28 | 4.6% | 16ゲームに1回 | 167 | 88 | 13,253 |
| 16ターン | 16 | 0.21 | 4.0% | 23ゲームに1回 | 300 | 115 | 12,818 |

**短くするほど良いわけではない。** ワイルドは1つの並びに1個までなので、
増えすぎると**かえって消せなくなる**。8ターンだと混合ロイヤルが2ゲームに1回で日常になる。

この表は「一番大きく消す立ち回り」での数字。**ロイヤルを狙う立ち回りだと桁が変わる**
（混合が1ゲームに2.9回）ので、倍率はそちらを基準に決めてある（→「ロイヤルの倍率と巻き戻し」）。

10ターンにしたのは、狙いに行ける密度を確保するため。12ターンだと狙っても素材が足りない。

あわせて**降ってくるワイルドは10%→4%に下げた**（昇格を主な供給源にするため）。

**既知の歪み**: キングロイヤルはクイーンロイヤルの約2倍出る（39 vs 83ゲームに1回）。
クイーンの方が機動力が高いぶん、揃う前に他のマッチに巻き込まれて消費されやすいと思われる。
倍率は同じ150倍なので、クイーンロイヤルの方が価値が高いのに報酬が同じ。優先度は低い。

### ロイヤルの倍率と巻き戻し（2026-08-09 実測で決定）

**倍率は「狙って作りに行ったときの出やすさ」で決めてある。**
昇格が入ってワイルドを自分で作れるようになった結果、ロイヤルは
「待っていれば出るもの」から「狙って作るもの」に変わった。
偶然の希少さ（＝一番大きく消す立ち回りでの出現率）で倍率を決めると実態と合わない。

`npm run royal-hunt` の実測（6×6・空振り禁止・昇格10ターン・各400ゲーム x 60ターン）。
「その種類のロイヤルを作りに行く」立ち回りを種類ごとに用意して比べた:

| 立ち回り | 混合 | クイーン | キング | スコア中央 |
|---|---|---|---|---|
| 一番大きく消す（比較用） | 3.1ゲームに1回 | 36ゲームに1回 | 14ゲームに1回 | 17,133 |
| ロイヤルを狙う（種類問わず） | **0.3ゲームに1回** | 1.4ゲームに1回 | 3.3ゲームに1回 | 45,442 |
| クイーンを狙う | 0.6ゲームに1回 | **0.8ゲームに1回** | 80ゲームに1回 | 40,598 |
| キングを狙う | 1.1ゲームに1回 | 50ゲームに1回 | **1.0ゲームに1回** | 33,420 |

1ゲームあたりの回数に直すと:

| 種類 | 狙ったとき | 狙わないとき | 狙う効果 | 倍率 | 巻き戻し |
|---|---|---|---|---|---|
| 混合 | 2.93回 | 0.33回 | 8.9倍 | **50** | **0.15** |
| クイーン | 1.24回 | 0.028回 | **44倍** | **110** | **0.35** |
| キング | 0.96回 | 0.070回 | 14倍 | **150** | **0.5** |

**狙わない場合と狙う場合で、クイーンとキングの順位が逆転する。**
狙わなければキングの方が2.5倍出やすいが、狙うとクイーンの方が1.3倍作りやすい。
クイーンは盤面のどこへでも動けるので揃えに行けるため。
以前「単一は一律150倍」だったのは、キングには妥当でクイーンには過大だった。

**巻き戻し（`royalRewind`）は順序だけが倍率と対応していて、絶対値は尺で決まる。**
上手いプレイヤーはロイヤルを1ゲームに何度も出すので、強く巻き戻すと
ラウンドが進まず**ゲームが終わらなくなる**（実測: 強さx1.0で6〜17%が150ターンでも終わらない）。
到達ラウンドは7〜11なので、この値でも戻る量ははっきり違う
（8ラウンド目なら 混合1 / クイーン2 / キング4 ラウンド戻る）。

**ロイヤル狙いは最適戦略になっている**（スコア中央 17,133 → 45,442 で2.7倍）。
これは設計どおり。「チャンスに気づいて取る」が腕の差になる。

### ランクと点数（2026-08-09 実測・変更しない結論）

**ランクによる点数の差は付けない。**

出現率は揃えてある（ランク1・2とも45%）が、**揃えやすさは倍ちがう**。
`npm run rank-balance` の実測（250ゲーム x 60ターン ＝ 14,981ターン）:

| | 盤面に居る割合 | 消えたマスの割合 | 1ターンに「消せる手」 |
|---|---|---|---|
| ランク1（ポーン） | 46.7% | 44.1% | **6.2手** |
| ランク2（ルーク等） | 46.5% | 55.9% | **12.2手** |

原因は機動力。ルーク・ビショップはどこへでも飛べるが、ポーンは斜め1マスしか動けない。

**それでもランク1を高くしてはいけない。** ポーンを消す動機が強まると、
昇格（＝ワイルドの主な供給源）が痩せる。実測:

| | ポーンを消した割合 | 昇格/ターン | ワイルド率 | 混合ロイヤル/ゲーム | 単一/ゲーム | スコア中央 |
|---|---|---|---|---|---|---|
| **ボーナスなし（現行）** | 40.5% | **0.50** | **5.3%** | **1.36** | **0.43** | **29,293** |
| ランク1を1.5倍 | 42.4% | 0.47 | 5.1% | 1.14 | 0.28 | 25,977 |
| ランク1を2倍 | 45.3% | 0.44 | 4.9% | 0.78 | 0.25 | 22,977 |

2倍にすると**混合ロイヤルが43%減り、スコアも下がる**。
「ポーンの価値」はすでに昇格という形で表現されている
（消さずに守れば10ターンでワイルドになる）。点数でも優遇すると二重取りになり、
「取る／揃える」と「育てる」のぶつかりが消える。

### ロイヤルの盤面一掃（2026-08-06）

レア役が出ると、**盤面に残っている駒を全部ワイルドに変えてから一掃する**（`game.js` の
`transformToWild` → `wipeBoard`）。目的は2つ:

1. 一面がクイーン／キングになる**演出**
2. **盤面とノルマのリセット**（ノルマ側は既存の `royalRewind` が担当）

混合ロイヤルならクイーンとキングが混ざり、単一ロイヤルならその駒だけになる。
一掃したあとは `applyGravity` が「置いた瞬間に消えない駒」で埋め直すので、連鎖は暴走しない。

`royalWipe: false` で無効にできる。

**点数は仮置き**（`royalWipeMultiplier: 1`）。実測で1回のクイーンロイヤルが
**23,120点**入った（ロイヤル役18,000点＋一掃5,445点。ノルマは450）。
桁が合っていないが、これはノルマと一緒に最後に調整する方針（shota 判断）。

### もうすぐ昇格するポーンを光らせる

`promoteAfter` まで残り2ターンを切ったポーンを金枠で脈打たせる（`ripeningPawns`）。

昇格が見えないと「守った結果」ではなく「勝手に起きたこと」に見えてしまい、
実際に shota から「残すとかじゃなくて勝手に増える」という指摘を受けた。

**注意**: 盤面には常時16個ほどポーンがあり全部が同時に歳を取るので、
閾値を伸ばしても1ターンあたりの昇格数はあまり下がらない
（6ターンで1.32回/ターン、8ターンで0.83回/ターン、12ターンで0.40回/ターン）。
数字ではなく「見えること」が体験を決める。

`npm run royal-hunt` — 種類ごとに「作りに行く」立ち回りを比べる。
`npm run rank-balance` — ランクごとの揃えやすさを比べる。

### 画面の構成（2026-08-19）

**ホーム画面 → ゲーム画面 の2枚。** ゲーム画面には盤面と状況だけを置き、
設定・説明・履歴はメニューの中にまとめる。
遊んでいる最中に目に入る情報が多いほど、盤面を読む邪魔になる。

| ホーム画面 | ゲーム画面 | メニュー（☰） |
|---|---|---|
| タイトル | タイトル / ☰ | はじめから / ホームへ / あそび方 |
| **はじめる** | スコア・ラウンド・残りターン | 音 / BGM |
| **モード選択** | ノルマのバー | この回の記録（最大連鎖・手数） |
| 最高スコア | メッセージ | 駒の動き確認表 |
| あそび方 / ルールと設定 | 盤面 | ルール / ランク / ノルマ / スコア |
| | レア役のバナー | プレイ履歴（書き出す・消す） |

**モード選択はホームだけに置く。**2か所にあると食い違う。
ゲーム中に変えたくなったら「ホームへ」で戻る（モード変更はどのみち作り直しになる）。

**チュートリアルの初回再生は「はじめる」を押したとき**に判定する。
以前は起動と同時に始めていたが、ホーム画面の後ろで勝手に進んでしまう。
あわせて「はじめる」のタップが **iOS の音の解禁**も兼ねている。

- メニューから「はじめから」「モード変更」を押したら**閉じて盤面に戻す**
- **チュートリアル中はメニューを開かせない**（「とばす」「NEXT」だけに集中させる）
- 閉じるのは ✕ / 背景タップ / Esc の3つ
- レア役のバナーは `z-index: 10`、メニューは `50`。メニューが上に来る

`test/layout.test.js` が `index.html` を直接読み、
**`<main>` の中に設定・説明が戻っていないこと**を検証する。
`test/boot.test.js` はスタブのイベントを発火させて、実際に開閉するかを見る。

### ルール説明の数字は実装から作る（2026-08-09）

`index.html` のルール説明に出る数字（昇格ターン・ノルマ・倍率・連鎖・点数）は
**手で書かず `main.js` の `fillRuleNumbers` が実装から埋める**。

これは動き確認表と同じ方針で、実際に食い違いが起きたから入れた。
`promoteAfter` を12→10にし、単一ロイヤルを150→110/150にし、ノルマをモード別にしたとき、
説明文は「12ターン」「150倍」「15%ずつ」のまま残っていた。
**ルールを変えるたびに説明文を直すのは必ず忘れる。**

`test/boot.test.js` が「? のままのマスが無いこと」を検証する。

### プレイ履歴のログ（2026-08-09）

`src/telemetry.js`。**端末の中（localStorage）だけに残す。どこにも送っていない。**
目的は2つ: ①自分のプレイを後から分析する ②将来ユーザーの傾向を集める土台にする。

1手ごとに残すもの:

| 項目 | 意味 |
|---|---|
| `f` / `to` / `p` / `c` | どの駒をどこからどこへ動かしたか |
| `n` / `ch` / `pts` / `k` | 消えたマス数 / 連鎖 / 点数 / レア役 |
| `o` | **その手番に指せた手の数** |
| `b` | **その手番で一番多く消せた数** |
| `ms` | その手にかけた時間 |

`o` と `b` が肝。`n >= b` なら最善手を選べたことになるので、
**シミュレーションの「上手いプレイヤー」が作り物なのに対して、実際の人間の腕が測れる。**
`ms` は「1ターン4.3秒」という尺の前提そのものの答え合わせになる。

記録は**指す前の盤面**で測る（実測0.42ms/ターンなので体感には出ない）。
チュートリアルは決められた手しか指せないので記録しない。

保存できない環境（プライベートモード・容量オーバー）では**黙って記録をやめる**。
ログのために遊べなくなるのが一番まずい。

画面は「ルール説明 → プレイ履歴」に書き出し／消去のボタン。
書き出しは **共有シート → ダウンロード → クリップボード** の順に試す。
iOS の WKWebView では `<a download>` が効かないことがあるため。

分析は `npm run analyze-log <書き出したJSON>`。

### 手が止まったときのヒント

10秒操作が無いと、**消せる手をひとつ選んで「動かす駒」だけ**を白枠で光らせる。
行き先まで見せると答えそのものになるので駒だけに留めている。
消せる手が無いときだけ、普通の合法手にする。

**一度出したら、そのターンは何度触っても消さない**（`noteActivity`）。
触るたびに消える作りだと、光った駒を確かめようとタップした瞬間に消えてしまう。
消えるのは実際に1手指したときだけ。

以前あった「ロイヤルチャンスの予告」は廃止した（2026-08-06）。

### 駒の動き確認表

`src/movement-chart.js`。**ルール説明の一番上**に置く。
上に駒のマークを並べ、押すと下の5×5の盤がその駒の動きに切り替わる。
盤を6つ並べると場所を取るので、盤は1つだけにしている。

**表は手で描かず、実際の移動判定（`movableSquares`）から作る。**
中央に調べたい駒、周りを全部「違う色の駒」で埋めた盤で `movableSquares` を呼べば、
返ってきたマスがそのまま動ける範囲になる。手で描くとルールを変えたときに古くなり、
嘘を教えることになる。

### 見た目のルール（2026-08-05）

**ランクは数字ではなくオーラの色で表す。白黒でも色を分ける。**

消えるのは「同じ色 かつ 同じランク」なので、オーラの色が同じ駒どうしが
3つ以上並べば消える、と見た目だけで判断できる状態にしてある。

| ランク | 白 | 黒 |
|---|---|---|
| 1 | 明るい水色 | 濃い青 |
| 2 | 黄緑 | 濃い緑 |
| 3 | ピンク | 濃い紫 |
| ワイルド | 白金 | 濃い橙（一段強く光らせる） |

ランクごとに同系色にしてあるので、**色味でランク・明るさで白黒**が読める。
色は `style.css` の `--rank1-white` 〜 `--wild-black` に集約。
数字のバッジは廃止した（情報量が増えて見づらいという指摘のため）。

**駒の記号には U+FE0E を付けること**（`src/pieces.js`）。付けないと iOS で
`♟` だけが絵文字として描画され、CSS の色が効かなくなる（→ 課題11）。

**マスのハイライトは「地の色ごと塗り替える」**（枠や半透明の重ねでは分かりにくい）。
駒とオーラはそのまま見えるので、移動先の駒の色も判別できる。

| 状態 | 色 |
|---|---|
| 移動できるマス | 鮮やかな金 `--movable-bg` |
| 選択中のマス | 鮮やかな水色 `--selected-bg` |
| レア役が決まる移動先 | ほぼ白 `--royal-bg`（脈動） |

盤の色はくすんだ茶系なので、彩度で明確に差がつく。

**レア役の名前は画面中央に大きく出す**（`main.js` の `showRoyalBanner`）。
名前は「ロイヤル」（混在）/「クイーンロイヤル」/「キングロイヤル」。
盤面が色で埋まっているので、背後を暗くしないと文字が読めない（radial-gradient のスクリム）。

**得点は消えた場所そのものに浮かび上がらせる**（`ui.js` の `popPoints`）。
カタマリごとに点数を出すので、`applyMove` が返す消去フェーズには
`groups: [{ cells, kind, points }]` が入っている。
上部のメッセージは点数を出さず「何連鎖か」「レア役か」だけを伝える。
経緯: 最初は中央に黄色い丸を描いていたが丸が駒を隠して色が判別できず、
次に半透明の重ねにしたが選択中と移動先が同系色で
「選んだ駒自身も移動先の一つ」に見えていた（→ 課題10）。

### 消去ルール

> 同じ色の駒が縦か横に3マス以上連続し、**ランクが揃っていれば**消える。
> クイーンとキングは**ワイルド**でどのランクの代わりにもなるが、1つの並びに**1個まで**。

- 消える: `111` `222` `333` `11W` `1W1` `W11` `22W` `33W` `WWW`
- 消えない: `112` `121` `123` `223`（ランク混在） / `1WW` `W1W` `2WW`（ワイルド2個）

`WWW`（クイーン・キングだけの並び）は消える。
同じランクなら駒の種類は違ってよい（ナイトとビショップは混ざれる）。

4マス以上でも条件を満たせばまとめて消える（`111W` は4マスとも消える）。
条件を満たすのが一部だけなら、その部分だけ消える（`1112` は左3マス）。
斜めは判定しない。

### ルールの変遷（2026-08-04）

1. 初版「最小ランク以外の駒が1個以下」（`112` `121` が消せた）→ 消えやすすぎて連鎖が暴走
2. 「ランク一致＋ワイルド1個まで」に変更、クイーン・キングをワイルド化
3. ランクを2段階（ポーン / それ以外）から3段階に分割 → 実質4種類だったのが6種類になった

### 移動ルール

- 駒はチェス本来の移動範囲のうち、**自分と違う色の駒がいるマス**にだけ移動できる
- **駒越えのブロックは一切ない**。間に何があっても射線は通る（同じ色の駒も飛び越える）
- 移動先の駒は取られて盤から消える。移動元のマスは空きマスになる
- ポーンは「取る動き」しか使わないので**斜め1マスのみ**。**前後どちらの斜めも取れる**（色による向きの違いは無い）
- 移動は白黒どちらの駒でも自由に選べる

### 消去後の処理

1. 移動を実行（取った駒が消え、移動元が空く）
2. 移動直後の盤面で消去判定（プレイヤーが狙った位置のまま判定される）
3. 消去 → 重力で下に落として上から新しい駒を補充 → 再判定（連鎖）
4. 消えなくなるまで繰り返す

**補充する駒は「置いた瞬間に消えない駒」から選ぶ**（`board.js` の `spawnPiece`）。
ランダムに補充すると補充した駒がそのまま消えて連鎖が止まらなくなる。
落下してきた既存の駒どうしが揃って起きる連鎖は、これまでどおり発生する。

**初期盤面はさらに「同じランクが3つ以上並ばない」を満たす**（色は問わない）。
ランクをオーラの色で見せているので、色違いで消えない並びが最初から見えていると
「揃っているのに消えない」と誤解されるため。途中の補充ではこの制限はかけない
（落下で偶然できる並びまでは避けられないので）。

端から順に埋めるので、6×6ではまれに「どの駒を置いても条件を満たせない」マスが出る（約2%）。
その場合は盤面ごと作り直す（`BOARD_RETRY`）。実測2000盤面で違反0件。

### スコア

消えたマスを**隣接しているものどうしでまとめ**、カタマリごとに計算する（`src/score.js`）。

```
1カタマリの点数 = マス数² × 5 × 連鎖倍率 × レア役の倍率
連鎖倍率 = 1.5^(連鎖数 - 1)
```

| 消し方 | 点数 |
|---|---|
| 3マス | 45 |
| 4マス | 80 |
| 5マス（縦横が交差した十字など） | 125 |
| 3マス・2連鎖目 | 68 |
| 3マス・5連鎖目 | 228 |
| **♛♚ ロイヤル**（クイーン・キング混在3マス）×50 | **2,250** |
| **♛ クイーンロイヤル**（クイーンだけ3マス）×110 | **4,950** |
| **♚ キングロイヤル**（キングだけ3マス）×150 | **6,750** |

設計の意図:

- **マス数の2乗**にしたのは、盤面を読む意味を作るため。以前は `消えた数 × 10` で
  3マス30点・5マス50点しかなく、多く消せる手を探しても報われなかった
- **連鎖倍率は等比で x1.5**。大連鎖は滅多に来ないので見返りを大きくしてある
  （8連鎖で17倍）。ただし上げすぎ厳禁 — 下の「連鎖倍率の上限」参照
- **ロイヤル役の倍率は「狙ったときの作りにくさ」で決めた**（下記「ロイヤルの倍率」参照）。
  昇格が入ってからは「出たらラッキー」ではなく**狙って作るもの**になったので、
  偶然の希少さで倍率を決めると実態と合わなくなった

### 連鎖倍率の上限（重要）

連鎖は1つ伸びるごとに頻度が約0.29倍に減る。倍率 g を等比で掛けると:

- **期待値**が発散しない条件: `0.29 × g < 1` → **g < 3.4**
- **分散**が発散しない条件: `0.29 × g² < 1` → **g < 1.86**

期待値より先に分散が壊れる。実際 g=2 で試すと1ターン148,775点（30ゲーム分）が出て、
**平均スコアが「下手なプレイ」の方が高くなる**逆転まで起きた。

`npm run chain-growth` の実測（150ゲーム x 40ターン）:

| 倍率 | 1〜5連鎖 | 上手い | 下手 | 腕の差 | 平均/中央値 | 最大1ターン | 連鎖ぶん |
|---|---|---|---|---|---|---|---|
| x1 | 1 1 1 1 1 | 4,325 | 3,195 | 1.35 | 1.08 | 18,135 | 28% |
| x1.2 | 1 1.2 1.4 1.7 2.1 | 4,698 | 3,431 | 1.37 | 1.16 | 55,857 | 34% |
| **x1.5** | 1 1.5 2.3 3.4 5.1 | 5,265 | 4,291 | 1.23 | 1.15 | 27,300 | 46% |
| x1.8 | 1 1.8 3.2 5.8 10.5 | 5,933 | 4,942 | 1.20 | 1.42 | 190,701 | 59% |
| x2 | 1 2 4 8 16 | 6,175 | 5,475 | 1.13 | 1.12 | 21,235 | 52% |
| x2.5 | 1 2.5 6.3 15.6 39.1 | 8,039 | 6,880 | 1.17 | 1.37 | 120,388 | 68% |

**倍率を上げるほど腕の差は単調に消える。** 連鎖は落下の運なので当然で、
「大連鎖を派手にすること」と「実力ゲーであること」は直接のトレードオフ。
x1.5 は脳汁側に寄せた妥協点。実力側に寄せたければ x1.2（腕の差1.37 / 連鎖ぶん34%）。

なお同じ「レアだから倍率を上げる」でも、ロイヤル役（`♛♛♛`）では8倍にできた。
あちらは発生が独立した離散イベントで頻度が固定なので、平均も分散も素直に動くため。
連鎖は長さ自体が指数分布なので「指数 × 指数」になり、分散が先に壊れる。

### 大規模シミュレーション（3000ゲーム x 40ターン ＝ 119,593ターン）

`npm run simulate 3000 40` の結果。毎ターン一番多く消せる手を選ぶ「上手いプレイヤー」想定。

**1ゲームの終わり方はシミュレーション側の便宜的な定義**（本来の終了条件が未実装のため）:
40ターン指したら終了、ただし消せる手が0になったらそこで打ち切り。
スコアはターン数にほぼ比例するので、ノルマ制を入れたら測り直すこと。

```
1ゲームのスコア分布
  下位10%   3,977
  中央値    5,068
  上位10%   7,089    ← 下位10%との差はわずか1.8倍。通常プレイは非常に安定
  上位1%   31,035    ← ここから先はレア役の世界
  最高     61,061
  平均      5,754    (中央値の1.14倍)

レア役
  ♛♚ ロイヤル :  19ゲームに1回   スコア寄与 3.3%
  ♛♛♛        : 115ゲームに1回   スコア寄与 3.5%
  ♚♚♚        : 100ゲームに1回   スコア寄与 4.5%
  合計                            スコア寄与 11.3%

手詰まり（消せる手が1つも無い）: 34 / 3000ゲーム (1.1%)
```

**通常プレイは実力で安定し、レア役だけが跳ねる**という狙いどおりの形になっている。
一方で「毎ゲームほぼ同じスコアになる」＝単調でもある（下記の課題7）。
なおこの計測はリーチの可視化を入れる前のもの。

## 構成

```
index.html      画面
style.css
src/pieces.js   駒とランクの定義、出現比率
src/rules.js    調整用パラメータ（連鎖倍率・ノルマ）
src/board.js    盤面生成・重力・補充
src/match.js    消去判定・カタマリ分け
src/moves.js    移動可能マスの計算
src/score.js    得点計算・レア役の判定
src/game.js     1手の処理（移動→消去→重力→連鎖→ノルマ判定）。DOMには触らない
src/ui.js       盤面の描画
src/main.js     入力とゲームロジックの接続
src/hints.js    「あと1手でレア役」の検出
src/telemetry.js プレイ履歴の記録（localStorage。DOMには触らない）
test/           node:test によるユニットテスト
tools/          バランス計測スクリプト
ios/            iOS版（WKWebViewでWeb版を表示するラッパー）。詳細は ios/README.md
```

ロジック（`game.js` まで）はDOMに依存させない。UIは `applyMove` が返す「フェーズの配列」を順に再生するだけ。

## 開発コマンド

```
npm test           # ユニットテスト
npm start          # http://localhost:8000 で確認（ES modules なので file:// では動かない）
npm run bench      # 消えやすさ・連鎖・スコアのざっくり計測
npm run simulate   # 大量自動プレイ。スコア分布・連鎖分布・レア役の発生率
npm run tune       # ノルマのパラメータを総当たり比較（旧。quota-tune を使うこと）
npm run chain-growth  # 連鎖倍率を比較
npm run quota-tune    # ノルマを尺（3分/5分）から逆算する。記録を .cache/ に残す
npm run verify-length # 本物のゲームループで尺を答え合わせする
npm run royal-hunt    # 種類ごとに「ロイヤルを作りに行く」立ち回りを比べる
npm run rank-balance  # ランクごとの揃えやすさを比べる
npm run promotion-sweep  # 昇格ターンを比較
```

調整用のパラメータは `src/rules.js` に集約してある。
`createGame(rng, { ... })` で差し替えられるので、上のツールから総当たりで試せる。

iOS版のビルドは `ios/README.md` を参照。

## 音

`src/audio.js`。**音源ファイルを持たず Web Audio で合成している。**

- 読み込み待ちが無い（1手目から鳴る）
- 連鎖数に応じて音程を変えられる（ファイルだと音の数だけ用意することになる）

いま実装済みなのは**コンボ音（ウッドベースのピチカート）だけ**。
連鎖が伸びるほど音階を上がっていく（ブルーノート入りのマイナーペンタトニック）。

**スマホのスピーカーは200Hzより下がほとんど出ない**ので、のこぎり波で倍音を残し、
「基音が聞こえなくても音程は分かる」状態にしてある。純粋なサイン波にすると鳴らない。

`npm run preview-combo` で WAV に書き出して耳で確認できる（BGMは `npm run preview-bgm`）。

### BGM（2026-08-19）

`src/bgm.js`。**shota が用意した音源を場面ごとに切り替える。**

| 場面 | 曲 | 長さ |
|---|---|---|
| ホーム画面 | `midnight-in-blue.mp3` | 11分29秒 |
| ゲーム中 | `midnight-in-the-quiet-room.mp3` | 11分09秒 |
| ゲーム中 | `midnight-in-the-quiet-room-2.mp3` | 13分10秒 |

`assets/bgm/` に置いてある（合計16MB）。1ゲームが3〜5分なので**ループしない**。

**Web Audio ではなく `<audio>` で鳴らしている。** 1曲11〜13分あるので、
Web Audio で読むと全部デコードしてメモリに載ることになる（1曲で100MB超）。
`<audio>` なら流しながら再生できる。`preload: 'none'` にして、
開いた瞬間に16MB取りに行かないようにしてある。

- **ゲームを始めるたびに曲を選び直す**（`restart`）。**直前と同じ曲は選ばない**。
  続けて同じだと「1曲しか無い」ように聞こえる
- 場面の切り替えはクロスフェード（1.2秒）。無音を挟むと「止まった」ように聞こえる
- **効果音の間はBGMを引っ込める**（`duck`）。同じ低音域でぶつかって効果音が埋もれる
- `Audio` が使えない環境でも落ちない。音のために起動できなくなるのが一番まずい

**iOSでは `mp3` の MIME を返すこと**（`ios/Sources/BundleSchemeHandler.swift`）。
`application/octet-stream` のままだと `<audio>` が鳴らない。
`ios/project.yml` の コピー処理にも `assets/` を足してある。

以前あった合成のBGM（Web Audio で夜ジャズを組み立てるもの）は、
音源が用意できたので外した。必要なら git の 6970673 から戻せる。

未実装（shota が音源を用意する予定）:

- 移動音（木のチェスボードに駒を置く音）

## ノルマの逆算（2026-08-09 実施済み）

ノルマは「1ターンにいくら稼げるか」から逆算する値なので、稼ぎに影響する要素
（`promoteAfter` / ワイルドの出現率 / `chainGrowth` / `royalRewind`）を
いじるたびに無効になる。**それらが固まったので逆算した。**

`npm run quota-tune` — **記録して再生する方式**。ノルマはゲームの終わりを決めるだけで
1ターンの稼ぎ方には影響しないので、「ノルマ無しで150ターン回した記録」を1度取れば、
あとは設定を当てて「何ターン目で落ちるか」を数えるだけで済む。
1回のシミュレーションから何百通りでも試せる（記録は `.cache/` に置く）。

`npm run verify-length` — こちらは**本物のゲームループで通しプレイ**して答え合わせをする。
再生方式は近似なので、最後は必ずこれで確かめること。実測は予測と一致した。

### 決めるときに効いた構造

**ノルマは「最悪の5ターン」で落とすのに、上手さは「最高の5ターン」に出る。**

| | 5ターンの下位10% | 中央 | 上位10% |
|---|---|---|---|
| 普通（一番多く消す） | 508 | 987 | 2,308 |
| 上手い（ロイヤルも狙う） | 488 | 1,153 | **9,551** |

下位10%はほぼ同じ。**繰り越しが無ければ腕の差はほぼゼロになる。**
以前「一番効いたのは成長率ではなく繰り越しの有無」と出たのは、これが理由。

もう一つ、**巻き戻しを強くするとゲームが終わらなくなる**。
上手いプレイヤーはロイヤルを何度も出すので、そのたびにラウンドが戻る。
巻き戻しの強さは「レア役の価値」ではなく「尺」で決まる制約がある。

### 通しプレイの実測（各200ゲーム）

| モード | | ターン | 分 | 到達R | スコア中央 | 1R落ち |
|---|---|---|---|---|---|---|
| みじかめ | 上手い | 40 | 2.9分 | 7 | 22,105 | 10% |
| | 普通 | 25 | 1.8分 | 5 | 5,929 | 3% |
| | 初見 | 20 | 1.4分 | 4 | 3,049 | 25% |
| じっくり | 上手い | 70 | 5.0分 | 11 | 44,541 | 8% |
| | 普通 | 35 | 2.5分 | 7 | 8,764 | 2% |
| | 初見 | 30 | 2.1分 | 6 | 5,367 | 20% |

1ターン4.3秒換算（55ターン=4分・70ターン=5分の実測から）。
「初見」は消せる手からランダムに選ぶだけのプレイヤー。

**残っているのは点数の桁**。`royalWipeMultiplier` はまだ仮置きの1で、
一掃だけで5,000点前後入る。ノルマ（350〜）と桁が合っていない。

## 未解決の課題（要調整）

### 1. バランス調整の記録（解決済み）

シミュレーション（ランダムに50手 x 30ゲーム）で段階的に計測した結果:

| 段階 | ランダム盤面の消えるマス | 消えた手 | 平均連鎖 | 最大連鎖 | 平均スコア | 合法手数 |
|---|---|---|---|---|---|---|
| 初版ルール | 26.7 / 64 | 75.8% | 8.77 | 81 | 411,565 | 267 |
| ランク一致＋ワイルド | 17.7 / 64 | 60.5% | 3.33 | 21 | 18,749 | 213 |
| ＋ 消えない駒を補充 | 18.8 / 64 | 52.8% | 2.24 | 14 | 6,320 | 214 |
| ＋ ランク3段階 | 13.5 / 64 | 35.7% | 1.56 | 6 | 1,647 | 231 |
| ＋ 出現率を均等化 | **9.9 / 64** | **27.5%** | **1.46** | 8 | 1,040 | 252 |

「消せる手が1つも無いターン」は全段階を通して0%、手詰まりも0回。
つまり**常に消せる手はあるが、適当に指すと7割は空振りする**状態になった。

再計測は `npm run bench "調整の内容"`（`tools/bench.mjs`）。
ルールや出現率をいじったら必ず前後で比べること。仕様どおりに動くことと、
遊べるバランスであることは別物だった。

### 2. スコア式の改訂（2026-08-04）

「上手く指すと得をするか」を測ったところ、**腕の差がほとんど出ていなかった**ため改訂した。

| | 1ターンの平均スコア | 中央値 | 連鎖ボーナス(運)の割合 |
|---|---|---|---|
| 旧式 下手に指す | 70 | 30 | 55% |
| 旧式 上手く指す | 78 (+11%) | 40 | 49% |
| 新式 下手に指す | 96 | 45 | 46% |
| 新式 上手く指す | **131 (+36%)** | **80 (+78%)** | 36% |

「下手に指す」＝消せる手からランダム、「上手く指す」＝一番多く消える手を選ぶ。

腕の差が出るようになった根拠として、選択の余地は元々あった:

```
そのターンで一番多く消せる手の消去マス数
  3マス 40.6% / 4マス 33.1% / 5マス 22.4% / 6マス以上 3.9%
  → 約6割のターンで4マス以上が存在する
消せる手ぜんぶで見ると 92% が3マス消し
  → 4マス以上は22手中1〜2手。「探せば見つかる」構造になっている
```

### 3. ランクごとの難易度は同じではない

出現率はランク1〜3すべて30%だが、実プレイでの消去回数は偏る:

```
ランク1（ポーン）  21.7%   ← 一番揃えにくい
ランク2（ナイト等）35.6%
ランク3（ルーク）  42.3%   ← 一番揃えやすい
```

原因は**駒の機動力**。ルークはどこへでも飛べるので揃えやすく、ポーンは斜め前2マスしか
動けないので揃えにくい。**高ランクほど難しい、ではない**ので、ランクに応じて点数を
変えるのは筋が悪い（一番簡単なルークに一番高い点を与えることになる）。

### 4. ノルマ制（実装済み）

5ターンごとにスコアのノルマを判定し、届かなければゲーム終了。ノルマはラウンドごとに上がる。
設定は `src/rules.js`、検証は `npm run tune`。

| 設定 | 値 |
|---|---|
| 判定間隔 | 5ターン |
| 初期ノルマ | 300 |
| 上昇率 | ラウンドごと x1.15 |
| 超過分の繰り越し | あり |

16パターンを200ゲームずつ比較した結果（上手い＝一番多く消せる手、下手＝消せる手からランダム）:

| 設定 | 上手い 到達R / ターン | 下手 到達R / ターン | 腕の差 | 1R落ち |
|---|---|---|---|---|
| 5T / x1.05 / 繰越あり | 29R / 145 | 20R / 100 | 1.45 | 1% |
| 5T / x1.10 / 繰越あり | 15R / 75 | 11R / 55 | 1.36 | 1% |
| **5T / x1.15 / 繰越あり** | **11R / 55** | **7R / 35** | **1.57** | 1% |
| 5T / x1.25 / 繰越あり | 8R / 37 | 6R / 30 | 1.33 | 2% |
| 5T / x1.15 / 繰越なし | 5R / 25 | 3R / 15 | 1.67 | 1% |
| 10T / x1.15 / 繰越あり | 10R / 100 | 7R / 70 | 1.43 | 1% |

**一番効いたのは成長率ではなく繰り越しの有無だった。**
繰り越さないと、プレイヤーの火力が成長しない以上「ノルマが実力を超えた瞬間に即終了」となり、
成長率をどう変えても一律4〜7ラウンドで終わる。繰り越すと余剰が貯金になって寿命が伸び、
成長率で調整できるようになる。狙って出せない大連鎖がここで効いてくるのも都合が良い。

「消せる手が1つも無い」場合も終了になる（3000ゲーム中34回＝1.1%）。

**注意: 完全にランダムにクリックするだけだと1ラウンド目で落ちる**（実測78%）。
ノルマは「消せる手を見つけられる」プレイヤーを前提にしている。詳しくは課題8。

**「消せる手にしか動けない」制限は入れない方針**。ノルマ制なら空振りが
ターンの浪費という形で自然にペナルティになる。むしろ
「次の手のために盤面を整える空振り」を戦略として残したい。

### 5. 選択肢が多すぎる（6×6モードでほぼ解決）

駒越えを認めた結果、8×8では1手あたりの合法手が**平均263手**ある。
「どこを押しても消える」状態は解消したが、今度は
「消せる手を263手の中から探す」作業になっていた。

**6×6モードでは87手まで減る**（1/3）。盤面を読み切れる量になった。

| モード | ランダム盤面の消えるマス | 合法手 |
|---|---|---|
| 8×8 / 3段階 | 10.3 / 64 (16%) | 263 |
| 6×6 / 2段階 | 8.5 / 36 (24%) | 87 |

8×8を残すなら、駒越えの射程を「3マスまで」に区切る案がまだ有効。

### 5b. モードごとの性格の違い（2026-08-05 実測）

`npm run tune` の結果（150ゲーム x 3種類のプレイヤー）:

| モード | 上手い R/ターン | 普通 R/ターン | 腕の差 | 初見の1R落ち |
|---|---|---|---|---|
| 8×8 初期300 | 11 / 55 | 8 / 40 | 1.38 | 75% |
| 6×6 初期300 | 17 / 85 | 13 / 65 | 1.31 | 56% |
| **6×6 初期450** | **11 / 55** | **6 / 30** | **1.83** | 71% |
| 6×6 初期600 | 7 / 35 | 2 / 10 | 3.50 | 83%（上手いも14%落ちる） |

6×6 はランクが2段階しかなく揃いやすい（ランダム盤面の24%が消える。8×8は16%）。
8×8と同じノルマ300だと17ラウンド85ターンまで延びてしまうので、**初期450**にして尺を合わせた。
その結果、**腕の差は8×8（1.38倍）より6×6（1.83倍）の方が大きい**。
合法手が少なく1手の重みが増すぶん、選択の質が効くようになったと考えられる。

### 6. リーチの可視化（実装済み）

`src/hints.js` の `findRoyalChances` が「あと1手でレア役が決まる手」を全部返す。
画面ではその駒を金色に脈打たせ、選ぶと決まる移動先が光る。

ワイルドだけのカタマリを作れるのはワイルドを動かしたときだけなので、
探索はワイルドの駒（盤面の1割）に限定してよい。実測で**平均0.12ms / 最大1.41ms**。

チャンスの頻度（4000ターン計測）:

- ロイヤル（混在）のチャンスがあるターン: **5.1%**（20ターンに1回）
- 単一種（`♛♛♛`/`♚♚♚`）のチャンス: **1.41%**（71ターンに1回）

**これは見た目だけの変更ではなくバランスを動かす。** プレイヤーがチャンスを毎回取ると:

| | ロイヤル発生 | スコア中央値 | 到達ラウンド |
|---|---|---|---|
| 狙わない | 0.06回/ゲーム | 5,112 | 11 |
| 毎回狙う | **0.90回/ゲーム** | **6,740（+32%）** | **14** |

1ゲームが55→70ターン（約4→5分）に伸びるが許容範囲。
むしろ「チャンスに気づいて取る」が新しい腕の差になるので、実力ゲームとしては良い方向。

### 7. 毎ゲームのスコアが似すぎている（リピート性の課題）

スコアの下位10%〜上位10%が 3,977〜7,089 と1.8倍しかない。つまり
**96%のゲームは「いつも通り」で終わる**。実力ゲーとしては良いが、
また遊びたくなる理由が弱い。

残っている手:

1. **記録の保存**（軽い）。最高スコアとレア役の遭遇回数を localStorage に残す。
   `♛♛♛` は滅多に出ないので、図鑑的に「まだ見ていない」が動機になる
2. 〜リーチの可視化（課題7で実装済み）〜
3. 〜ノルマ制（課題4で実装済み）〜

### 8. チュートリアル（実装済み）

`src/tutorial.js` に固定盤面とステップ定義。3ステップ。

1. 動かす（白ルークで違う色の駒を取る。この手では消えない）
2. 消す（白のランク2を3つ並べて消す）
3. ワイルド（**黒の**クイーンをポーン2つに合わせて消す）

3で黒の駒を動かすのは、**「白も黒も動かせる」を同時に伝えるため**。
チュートリアルで白しか動かさないと、白だけ操作できると誤解される。

**文章は使わない。光らせる場所だけで伝える**（2026-08-05 shota 指定）。

| 見た目 | 意味 |
|---|---|
| 白い枠が脈打つマス | 次にタップするところ |
| 緑の枠が脈打つマス | そこへ入れば揃う相手（タップするところではない） |
| 進み具合 | 文字ではなく点（●○）で示す |

例外的に文字を使うのは「NEXT」と「とばす」のボタンだけ。
1手が終わると NEXT が出て、押すまで次へ進まない（結果を見る時間を作るため）。

仕様:

- **「駒を選ぶ→動かす」をひと続きで行う**。選ぶだけで終わるステップは作らない
- **正解の手だけ受け付ける**（`isTutorialTap`）
- **ステップごとに盤面を作り直す**ので、前のステップの結果に左右されない
- 初回起動時に自動で始まる（`localStorage` の `chess-puzzle.tutorial-seen`）。
  「あそび方」ボタンでいつでも再生できる
- チュートリアル中はスコアとノルマを隠す（意味が無く混乱するため）

`test/tutorial.test.js` が、**光らせる「揃う相手」と実際に一緒に消えるマスが一致すること**を
検証している（盤面をいじったときにズレると、教えている内容が嘘になるため）。
ほかに「各ステップの盤面に消え残りが無い」「1手目では消えない」「2・3手目でちょうど3マス消える」
「黒の駒を動かすステップがある」も検証済み。

**消えたあとは案内のハイライトを消すこと**（`awaitingNext`）。
消えたマスには別の駒が落ちてくるので、光らせ続けると別の駒を指しているように見える。

**盤面の作り方のコツ**: 色を1マスおきに交互にしておけば同じ色が3つ並ばないので絶対に消えない。
消したい場所だけ意図的に交互を崩す。崩した場所は縦横とも3連続にならないか必ず確認する
（`test/tutorial.test.js` が全ステップの盤面に消え残りが無いことを検証している）。

### 9. 初見プレイヤーへの導線（残り）

完全にランダムなクリックだと1ラウンド目で78%落ちる。
**ただしノルマを甘くして解決してはいけない**。実測で、初期ノルマを下げると
初見の1R落ちは改善するが腕の差がほぼ消える（繰り越しがあるため全員が
序盤に貯金を作れて寿命が揃ってしまう）。

| 設定 | 上手い R | 普通 R | 初見 R | 腕の差 | 初見の1R落ち |
|---|---|---|---|---|---|
| **初期300 / x1.15（現行）** | 12 | 8 | 1 | **1.50** | 78% |
| 初期200 / x1.18 | 13 | 11 | 1 | 1.18 | 64% |
| 初期150 / x1.20 | 14 | 13 | 2 | 1.08 | 43% |
| 初期100 / x1.30 | 13 | 12 | 3 | 1.08 | 38% |

方針（2026-08-04 shota 判断）: **難易度ではなくチュートリアルで解決する。**
→ 基本のチュートリアルは課題8で実装済み。以下は「最初の数ゲームを甘くする」案で、未着手。

チュートリアルモードの構想:

- **ノルマ4ラウンドくらいで終了**する短いモード
- **連鎖しやすい駒の出し方**にして、スコアが出る快感を先に味わわせる
- **記録には残さない**（ハイスコアやレア役のカウントに含めない）
- 起動して最初の数ゲームがこれ。徐々に通常ルールに戻していく

リリース時にチュートリアルとセットで調整するため、今は保留。

### 11. 白ポーンが黒く描画されていた（2026-08-05 修正済み・重要）

**`♟` (U+265F) は iOS で絵文字として描画される。** 絵文字には CSS の `color` も
`-webkit-text-stroke` も効かないので、**白ポーンが黒ポーンの絵柄のまま表示されていた**。
チェスの駒でこの扱いになるのは U+265F だけ（♚♛♜♝♞ は通常の文字）。

修正: グリフの末尾に **U+FE0E（異体字セレクタ・文字表示）** を付ける（`src/pieces.js`）。

**これ1つで、それまでの報告3つがすべて説明できた:**

| 報告 | 実際に起きていたこと |
|---|---|
| 「黒駒が同色を取れる」 | 白ポーンを取ったのに、黒ポーンに見えていた |
| 「6×6で白のポーンが出ない」 | 生成はされていた。黒く描かれていただけ |
| 「3つ以上並んでも消えない」 | 同じ色に見えていた並びが、実際は白黒混在だった |

生成データ側は正常だった（両モード・全駒種で白50.0%前後、各20000サンプル）。

**教訓**: 「データは正しいのに見え方がおかしい」ときは、フォント／絵文字の描画を疑う。
Unicode の記号は環境によって絵文字扱いになり、CSS の色指定が効かなくなることがある。

### 10. 「同色に動ける」は誤解だった（2026-08-05 検証済み）

実機で「黒駒が同じ色のマスに動けているのでは」という指摘があったため検証した。

```
検査した移動先: 140,972 件（両モード x 800盤面の全駒）
同色に動ける件数: 0
ポーンの方向が違う件数: 0
```

さらに `applyMove` は不正な手に `null` を返して何も起こさないので、
**実際に動けた時点でその手は合法**（＝移動先は異色）だったことになる。

原因は表示側にあった（→ 課題11の絵文字問題が主因）。
加えて、選択中のマスと移動先ハイライトが同じ金色で見分けられず、
「選んだ駒自身も移動先の一つ」に見えていた。**選択中は水色**に変更して解消。

**教訓**: 「ルールがおかしい」という報告でも、まず不変条件を大量データで検証する。
2回とも表示の問題だった。ただし検証で終わらせず、見え方そのものを直すこと。

### 9. ポーンの移動方向（2026-08-05 修正済み）

**修正前は「白は上・黒は下」だったため、ポーンの41%が動けない状態だった。**
重力も下向きなので黒ポーンは最下段に溜まって二度と動けず、白ポーンは最上段で同じことが起きる。
盤面の3〜4割がポーンなので影響が大きい。

`斜め4方向すべてに取れる`（色による向きの違いを廃止）に変更して **41% → 17%** に改善。
残る17%は「斜め4マスが全部同色」のケースで、これは避けようがない。

チェスらしさは減るが、ポーンだけ色によって挙動が変わる必然性はこのゲームには無い
（色は「揃うかどうか」を決めるためのもの）。

### 10b. ポーンが動かしにくい（残課題）

斜め前2マスにしか動けず、しかもそこが同色だと動けない。
全部ポーンの市松模様の盤面では誰も動けなくなる（テストで確認済み）。
実際の盤面は色がランダムなので手詰まりにはならないが、ポーン（出現率30%）は
「消す対象にはなるが自分では動かしにくい駒」になっている。
